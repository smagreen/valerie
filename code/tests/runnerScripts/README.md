code/tests/runnerScripts
========================

This folder contains scripts used by the test-runner.

It's checked into GitHub to allow testling-ci to run the tests against a multitude of browsers.